# TimeTracker
TimeTracker is a Windows desktop application that tracks how much time you spend in different applications. It runs in the background, detects the currently focused window, and only counts time when you're actually active. Data can be exported to JSON for further analysis.
